This is a snapshot of a real production Python backend. FastAPI +
SQLModel + Postgres + Redis + a background worker pool, among other tech stack.

Pytest + dbmate + codegen scripts are all wired up. Postgres, Redis are running at 5432/6379 ports. dbmate is available for DB migrations.